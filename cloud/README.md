# Cloud Stack

Includes MQTT broker deployment, ingestion service, time-series storage integration, dashboards, and RBAC/idempotent command API.

Run:

```bash
cd cloud/deploy
docker compose up -d
```
